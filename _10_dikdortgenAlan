#include <stdio.h>
#include <stdlib.h>

int main()
{   float boy ,en, cevre, alan;

    printf("Dikdörtgen uzunluğunu giriniz:");
    scanf("%f",&boy);
    printf("Dikdörtgenin genişliğini giriniz:");
    scanf("%f",&en);
    //dikdörtgen çevresi
    cevre = 2*(boy +en );
    printf("Dikdörtgenin çevresi = %f birimdir \n",cevre);

    alan = boy*en;
    printf( "Dikdörtgenin alanı = %f birimdir \n",alan);

    return 0;
}
